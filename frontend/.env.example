# Public browser configuration only. Keep this stable to preserve encrypted local history.
VITE_TEEHO_PWD=teeho-community-local-history-v1
VITE_API_BASE_URL=/api
VITE_FRONTEND_PORT=8080
# Used by the local Vite development proxy, not embedded in browser code.
API_PROXY_TARGET=http://127.0.0.1:9634
