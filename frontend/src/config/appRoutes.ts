/** 社区应用的固定路由。 */
export const appRoutePaths = {
    login: '/login',
    register: '/register',
    workspace: '/workspace',
    skill: '/skill',
    skillAuthorize: '/skill/authorize',
    skillDevices: '/skill/devices',
} as const
